c++中用指针表示数组，但是不完全一样，
数组的名字可以当作是数组第一个元素的指针，
有一个数组array，用指针a指向它，即*a	那么a就是&array[0],	*a就是array[0].
就算直接输出数组名，cout<<array,那么输出的结果便是array[0]的地址。
不过指针是一种变量，和数组不一样。用sizeof输出指针得到指针的长度，而输出数组则会输出数组的长度。
若有一个int数组a，输出a 和 输出&a 的地址看起来完全一样，但实际上a是a[0]的地址，	而&a是整个数组a的地址。
另外，一个double类型的指针便可以指向double数组。
